{
  "name": "app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prepare": "node ./scripts/install-git-hooks.mjs && pnpm run build:packages",
    "dev": "cross-env NODE_ENV=development dotenv -e .env.dev -- pnpm run dev:app",
    "dev:app": "cross-env NODE_ENV=development dotenv -e .env.dev -- pnpm --parallel -F @zhblogs/api -F @zhblogs/web run dev",
    "dev:api": "cross-env NODE_ENV=development dotenv -e .env.dev -- pnpm -F @zhblogs/api run dev",
    "dev:web": "cross-env NODE_ENV=development dotenv -e .env.dev -- pnpm -F @zhblogs/web run dev",
    "build": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm run build:packages && cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm -F @zhblogs/api -F @zhblogs/web run build",
    "build:packages": "pnpm --filter @zhblogs/db --filter @zhblogs/utils --if-present run build",
    "build:api": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm run build:packages && cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm -F @zhblogs/api run build",
    "build:web": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm run build:packages && cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm -F @zhblogs/web run build",
    "preview": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm --parallel -F @zhblogs/api -F @zhblogs/web run preview",
    "preview:app": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm --parallel -F @zhblogs/api -F @zhblogs/web run preview",
    "preview:api": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm -F @zhblogs/api run preview",
    "preview:web": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm -F @zhblogs/web run preview",
    "start": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm --parallel -F @zhblogs/api -F @zhblogs/web run start",
    "start:app": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm --parallel -F @zhblogs/api -F @zhblogs/web run start",
    "start:api": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm -F @zhblogs/api run start",
    "start:web": "cross-env NODE_ENV=production dotenv -e .env.prod -- pnpm -F @zhblogs/web run start",
    "test": "cross-env NODE_ENV=test dotenv -e .env.test -- pnpm run test:all",
    "test:all": "cross-env NODE_ENV=test dotenv -e .env.test -- pnpm -r --if-present run test",
    "test:api": "cross-env NODE_ENV=test dotenv -e .env.test -- pnpm -F @zhblogs/api run test",
    "test:web": "cross-env NODE_ENV=test dotenv -e .env.test -- pnpm -F @zhblogs/web run test",
    "hooks:install": "node ./scripts/install-git-hooks.mjs",
    "commit:lint": "commitlint --from HEAD~1 --to HEAD",
    "db:studio": "dotenv -e .env.dev -- pnpm -F @zhblogs/db drizzle studio",
    "db:generate": "pnpm -F @zhblogs/db drizzle generate",
    "db:migrate:dev": "dotenv -e .env.dev -- pnpm -F @zhblogs/db drizzle migrate",
    "db:migrate": "dotenv -e .env.prod -- pnpm -F @zhblogs/db drizzle migrate",
    "util:psl:update-rules": "pnpm -F @zhblogs/utils run psl:update-rules"
  },
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.28.2+sha512.41872f037ad22f7348e3b1debbaf7e867cfd448f2726d9cf74c08f19507c31d2c8e7a11525b983febc2df640b5438dee6023ebb1f84ed43cc2d654d2bc326264",
  "devDependencies": {
    "@commitlint/cli": "^20.4.4",
    "@commitlint/config-conventional": "^20.4.4",
    "@types/node": "^25.5.0",
    "cross-env": "^10.1.0",
    "dotenv-cli": "^11.0.0",
    "tsdown": "^0.21.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
